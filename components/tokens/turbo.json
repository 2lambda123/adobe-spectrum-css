{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "pipeline": {
      "clean": {},
      "build": {
        "inputs": ["custom-*/*.css"],
        "outputs": ["dist/**"],
        "env": [ "NODE_ENV=production" ]
      }
    }
  }
