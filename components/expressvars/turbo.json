{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "pipeline": {
      "clean": {},
      "build": {
        "inputs": ["custom.css", "css/**/*.css", "json/spectrum-metadata.json"],
        "outputs": ["dist/**"],
        "env": [ "NODE_ENV=production" ]
      }
    }
  }
